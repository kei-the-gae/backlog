<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results for "<%= search %>"</title>
</head>

<body>
    <%- include('../partials/_navbar') %>
    <h1>Search Results for "<%= search %>"</h1>
    <p>Not finding the game you're looking for? <a href="/games/search">Go back</a> and try searching again.</p>
    <ul>
        <% games.forEach(game => { %>
        <li><a href="/users/<%= user._id %>/games/new"><%= game.name %></a> <a href="<%= game.url %>" target="_blank"
                rel="noopener noreferrer">IGDB</a></li>
        <% }); %>
    </ul>
</body>

</html>