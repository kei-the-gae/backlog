<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= game.name %></title>
</head>

<body>
    <%- include('../partials/_navbar') %>
    <h1><%= game.name %></h1>
    <a href="<%= game.url %>" target="_blank" rel="noopener noreferrer">IGDB</a>
    <p><%= game.summary %></p>

    <% if (user) { %>
    <% if (userGameData) { %>
    <p>Completion status: <%= userGameData.gameComplete %></p>
    <p>Achievement completion status: <%= userGameData.achievementsComplete %></p>
    <p>Notes: <%= userGameData.notes %></p>

    <a href="/users/<%= user._id %>/games/<%= game._id %>/edit">Edit this game</a>
    <form action="/games/<%= game._id %>?_method=DELETE" method="post">
        <button type="submit">Delete this game</button>
    </form>
    <% } else { %>
    <a href="/users/<%= user._id %>/games/new?game=<%= game.igdbId %>">Add this game to your collection</a>
    <% }; %>

    <% }; %>

</body>

</html>